# YOLO Detection Dataset Configuration
# ====================================
# Self-contained detection dataset — no symlinks needed.
#
# Label format: Standard YOLO detection (5 columns per object)
#   class_id x_center y_center width height
#
# Usage:
#   python3 train_detection.py
#   (or explicitly: python3 train_detection.py --data dataset_detection.yaml)

path: /Users/krys.petrie/dev/photoscan-import-cli/data_detection
train: images/train
val: images/val

nc: 1
names:
  0: photo